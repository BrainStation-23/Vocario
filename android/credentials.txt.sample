# Vocario App Keystore Credentials

**IMPORTANT: This is a sample file. Create a real `credentials.md` file with your actual keystore credentials.**
**Never commit the real credentials.md file and the keystore file to version control!**

## Keystore Configuration

The following credentials are required for signing the production APK/AAB:

```
storePassword: your_keystore_password_here
keyPassword: your_key_password_here
keyAlias: your_key_alias_here
storeFile: @tanim2025__vocario-keystore.bak.jks
```

## Instructions

1. Copy this file and rename it to `credentials.md` in the android directory
2. Replace the placeholder values with your actual keystore credentials
3. Ensure your keystore file `@tanim2025__vocario-keystore.bak.jks` is placed in the `android/` directory